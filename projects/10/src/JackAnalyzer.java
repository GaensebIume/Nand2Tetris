import java.io.*;

public class JackAnalyzer {
    public static void main(String[] args) {
        //if (!args[0].endsWith(".jack"))return;
        //Tokenizer tokenizer = new Tokenizer(new File(args[1]));
        Tokenizer tokenizer = new Tokenizer(new File("./Square/Main.jack"));
    }
}
